REM This filesystem is corrupt and has the 'panic on corrupt' bit set in the superblock
REM If your distro does not override this with error=remount-ro then plugging the drive in will cause a kernel panic
USB_MOUNT_DISK_READ_ONLY /badfs.img

REM At this point you could swap the filesystem to another, maybe a linux live environment